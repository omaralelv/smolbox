import { useNavigate } from 'react-router-dom';

function Bitacora() {
    return (
        <div style={styles.container}>
            {/* Espacio en blanco preparado para el desarrollo */}
        </div>
    );
}

const styles = {
    container: {
        width: '100%',
        minHeight: '60vh',
    },
};

export default Bitacora;